import type { ComponentProps } from "react";

type ResourceListModuleProps = ComponentProps<"div">;

export const ResourceListModule = (props: ResourceListModuleProps) => {
  const { className } = props;
  return "";
};
